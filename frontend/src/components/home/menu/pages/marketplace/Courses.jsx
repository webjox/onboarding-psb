import styled from "@emotion/styled/macro";

const Courses = () => {
    return <CoursesBody></CoursesBody>;
};

export default Courses;

const CoursesBody = styled.div`
    display: flex;
    flex-wrap: wrap;
`;
